<?php
/*	function FSGT_ajouter_boutons($boutons_admin) {
        <pipeline>
        		<nom>ajouter_boutons</nom>
            <inclure>FSGT_pipelines.php</inclure>
        </pipeline>

		// si on est admin
		if ($GLOBALS['connect_statut'] == "0minirezo") {
		  // on voit le bouton comme  sous-menu de "naviguer"
			$boutons_admin['naviguer']->sousmenu['cfg&cfg=FSGT']= new Bouton("plugin-24.gif", _T('Config Squelette FSGT') );
		}
		return $boutons_admin;
	}
*/
?>
